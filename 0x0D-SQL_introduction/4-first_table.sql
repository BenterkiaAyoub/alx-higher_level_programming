-- Script to create table first_table in the specified database

/* 
   Your task description or any relevant comments can be placed here.
   For example, this script creates the table first_table in the specified database.
*/

-- Make sure all SQL keywords are in uppercase, as per the requirements.

USE `hbtn_0c_0`;

CREATE TABLE IF NOT EXISTS first_table (
    id INT,
    name VARCHAR(256)
);

